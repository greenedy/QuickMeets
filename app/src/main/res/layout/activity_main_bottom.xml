<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">
    <ViewFlipper xmlns:android="http://schemas.android.com/apk/res/android"
        android:id="@+id/vf"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content" >

        <include android:id="@+id/map" layout="@layout/activity_main" />
        <include android:id="@+id/list" layout="@layout/activity_event_list" />
        <include android:id="@+id/profile" layout="@layout/activity_user_profile" />

    </ViewFlipper>

   <!-- <include layout="@layout/activity_main" />-->


    <android.support.design.widget.BottomNavigationView
        android:id="@+id/bottom_navigation_A"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        app:itemBackground="@color/colorPrimary"
        android:background="@color/colorPrimary"

        app:itemIconTint="@color/colorWhite"
        app:itemTextColor="@color/colorWhite"
        app:menu="@menu/bottom_navigation" />

</RelativeLayout>
